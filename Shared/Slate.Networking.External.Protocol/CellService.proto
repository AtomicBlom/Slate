syntax = "proto3";
option csharp_namespace = "Slate.Networking.External.Protocol.Cell";

service CellService {
	rpc Subscribe(stream UpdateToGameWarden) returns (stream UpdateFromGameWarden);
}

message UpdateToGameWarden {

}

message UpdateFromGameWarden {
	oneof Message {
		PlayerMovement PlayerMovement = 1;
	}
}

message PlayerMovement {
	
}
